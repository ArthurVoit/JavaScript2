let numero = prompt("fala um numero")
function verificarSinal(str){
    if(str.include("-")){
        console.log(str, "é negativo")
    }
    if(str != 0, str.include("-")){
        console.log(str, "é zero")
    }
    else{
        console.log(str, "é zero")
    }
}
verificarSinal(numero);